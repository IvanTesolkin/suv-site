/**
 * PostCSS configuration file.
 *
 * Tailwind CSS and Autoprefixer are used to generate the CSS used
 * throughout the application. Additional PostCSS plugins can be
 * configured here if required in the future.
 */
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}
  }
};